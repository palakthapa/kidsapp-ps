// import validationUtil from './utils/validationUtil'

export default function middleware(req, res, next) {
    // switch (req.nextUrl.pathname) {
    //     case '/api/login': validationUtil(req, res, 'login');
    // }
}