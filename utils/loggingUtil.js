export const logError = function (code, message, extra) {
    console.log(`Error Code: ${code}\nMessage: ${message}`);
}

export const logMessage = function (code, message, extra) {
    console.log(`Message Code: ${code}\nMessage: ${message}`);
}